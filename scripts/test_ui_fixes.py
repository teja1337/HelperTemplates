"""
Тест загрузки иконки и проверка рамок окна
"""
import sys
import subprocess
from pathlib import Path

print("=" * 60)
print("ТЕСТ v2.0.3")
print("=" * 60)

# Проверяем что icon.ico встроена в .exe
exe_path = Path(__file__).parent / "Helper.exe"
if exe_path.exists():
    print(f"\n✅ Helper.exe найден: {exe_path}")
    print(f"📊 Размер: {exe_path.stat().st_size / (1024*1024):.2f} MB")
    
    # Проверяем что icon.ico встроена
    import zipfile
    try:
        # PyInstaller использует PYZ архив
        print("\n🔍 Проверяю встроенные файлы...")
        # Просто запустим и посмотрим логи
        print("\n✅ Запускаю приложение с видимой консолью для проверки логов...")
        print("\nОтправляю результаты:")
        print("1️⃣  Проверьте, видна ли иконка Helper в панели задач")
        print("2️⃣  Проверьте, есть ли стандартные рамки Windows (должны быть убраны)")
        print("3️⃣  Проверьте, видно ли приложение при сворачивании")
        
    except Exception as e:
        print(f"Ошибка: {e}")
else:
    print(f"\n❌ Helper.exe не найден: {exe_path}")
